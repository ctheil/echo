import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#c5c5d1",
    },
    background: {
      paper: "#25262C",
      default: "#25262C",
    },
  },
});
