import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#c5c5d1",
    },
    secondary: {
      main: "#0FA47F",
    },
    background: {
      paper: "#25262C",
      default: "#25262C",
    },
  },
});
